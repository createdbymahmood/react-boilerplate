import { mapKeys } from 'lodash';

export const paths = {
    home: '/home',
    profile: `/profile/:id`,
    editProfile: '/profile/:id/edit',
    editProfileSettings: '/profile/:id/edit/settings',
    login: '/login',
};
